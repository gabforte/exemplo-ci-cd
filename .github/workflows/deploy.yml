name: Pipeline CI/CD - Github Pages

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  validacao-html:
  # continuar na proxima aula
  deploy-producao:    
  # continuar na proxima aula